name: XRP 12-Hour Report

on:
  schedule:
    - cron: '*/15 * * * *'
    - cron: '0 * * * *'
    - cron: '0 0 * * *'
  workflow_dispatch:

permissions:
  contents: write   # allows default token to push

jobs:
  run-report:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas requests discord-webhook tweepy==4.14.0

      - name: Run XRP Intel Script
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
          X_BEARER_TOKEN: ${{ secrets.X_BEARER_TOKEN }}
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_SECRET: ${{ secrets.X_ACCESS_SECRET }}
        run: python combined_xrp_intel_report.py

      # THE ONLY VERSION THAT CANNOT FAIL WITH CONCURRENT RUNS
      - name: Commit and push updated CSV (conflict-proof)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add xrp_history.csv

          # If nothing changed, exit
          if git diff --cached --quiet; then
            echo "No changes to xrp_history.csv – nothing to do."
            exit 0
          fi

          echo "New data detected – forcing latest version to repo..."

          # THIS IS THE MAGIC PART – no rebase, no conflicts ever
          git fetch origin main
          git checkout origin/main -- xrp_history.csv   # get absolute latest CSV
          git add xrp_history.csv
          git commit -m "Update XRP history CSV [skip ci]"

          git push origin main --force-with-lease

          echo "CSV successfully updated and pushed!"
